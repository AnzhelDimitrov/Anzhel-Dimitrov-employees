# Anzhel-Dimitrov-employees  
download repo  
load code with VisualStudioCode  
open terminal cd into the project directory  
npm install (to install all dependencies)  
npm run dev (to run the app in the browser)  
